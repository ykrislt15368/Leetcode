class Solution {
public:
    int findMaxK(vector<int>& nums) {
        int n=nums.size();
        int lr=-1;
        for(int i=0;i<n;i++){
            for(int j=i+1;j<n;j++){
                if(nums[i]+nums[j]==0) lr=max(lr,abs(nums[i]));
            }
        }
        
        return lr;
    }
};